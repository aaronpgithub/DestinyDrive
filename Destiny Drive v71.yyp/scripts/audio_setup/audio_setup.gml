function audio_setup() {
	//audio_sound_gain(sndExplosion, 0.4, 0)

	//audio_sound_gain(sndHurt1, 0.7, 0)

	//audio_sound_gain(sndShotPistol, 0.15, 0)

	//audio_sound_gain(sndShotSMG, 0.20, 0)

	//audio_sound_gain(sndShotRifle, 0.15, 0)

	//audio_sound_gain(sndShotLaser, 0.15, 0)

	//audio_sound_gain(sndSwampMusic, 0.7, 0)

	//audio_sound_gain(sndMenuMusic, 0.7, 0)

	//audio_sound_gain(sndPipDeath, 0.4, 0)

	//audio_sound_gain(sndDiseaseDeath, 0.4, 0)

	//audio_sound_gain(sndShotFlakPistol, 0.25, 0)



}
