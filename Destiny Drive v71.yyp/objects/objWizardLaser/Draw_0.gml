/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

if abs(image_yscale) != 1 and dying = false draw_sprite_ext(sprite_index, image_index, x, y, image_xscale, sign(y - road_y_mid), 0, c_white, 0.5)