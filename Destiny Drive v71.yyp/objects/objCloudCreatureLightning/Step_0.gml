/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

len += spd

if instance_exists(cloud_to_follow) {
x = cloud_to_follow.x + lengthdir_x(len, angle)
y = cloud_to_follow.y + lengthdir_y(len, angle)
}

if x < 0 instance_destroy()